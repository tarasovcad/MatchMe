import {Project} from "@/types/projects/projects";
import {User} from "@supabase/supabase-js";
import React from "react";

// name, current role, seniority, availability, skills, years of experience, Language Spoken,  Date of Request, status badge, invited by

const ProjectManagementRequests = ({project, user}: {project: Project; user: User}) => {
  return <div></div>;
};

export default ProjectManagementRequests;
